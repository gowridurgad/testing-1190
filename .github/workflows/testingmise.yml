name: testing mise

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:      
      - uses:  gperdomor/setup-node@feature/mise-support  
        with:
          node-version-file: mise.toml
      - name: Verify
        shell: bash
        run: |
          node --version
          
    
