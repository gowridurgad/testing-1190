name: testing mise

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:      
      - uses: actions/checkout@v7
      - uses:  actions/setup-node@v7
         
         
      - name: Verify
        shell: bash
        run: |
          node --version
          
    
